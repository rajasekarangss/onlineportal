sudo: false
language: java
install: true

jdk: openjdk8

git:
  depth: 500
addons:
  sonarcloud:
    organization: "rajasekarangss-github"
    token:
      secure: 04ca1fe7804e22c2f14e7d72fa0ec1cf93c01875

# before_install:
# - chmod +x mvnw

script:
# - "./mvnw clean install -B"
# - "./mvnw clean org.jacoco:jacoco-maven-plugin:prepare-agent package sonar:sonar"
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent package sonar:sonar

cache:
  directories:
  - "$HOME/.m2/repository"
  - "$HOME/.sonar/cache"
